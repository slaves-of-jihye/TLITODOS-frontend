/*
 * 화면 여러 곳이 함께 지키는 값들입니다.
 *
 * 그룹 이름 길이는 그룹을 만들 때와 이름을 고칠 때가 같아야 하는데, 그 둘은
 * 서로 다른 기능입니다 — 같은 층끼리는 서로를 부르지 않으므로 여기로 내려옵니다.
 */
export const GROUP_NAME_LIMIT = 20;

/** 할 일 제목. 디자인의 카운터가 0/40입니다. 목록에서 새로 쓸 때와 상세에서 고칠 때가 같아야 합니다. */
export const TODO_TITLE_LIMIT = 40;

export const GROUP_BIO_LIMIT = 80;
